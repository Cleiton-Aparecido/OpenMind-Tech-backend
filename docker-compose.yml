
services:
  db:
    image: postgres:15-alpine
    container_name: postgres_open_mindtech
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_DATABASE}
    ports:
      - '5968:5432'

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nest_open_mindtech
    depends_on:
      - db
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '${PORT}:${PORT}'
    command: >
      sh -c "
        sleep 5 &&
        yarn build &&
        yarn migrate:run &&
        yarn start:prod
      "

volumes:
  postgres_data:
